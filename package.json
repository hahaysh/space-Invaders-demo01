{
  "name": "orbit-defender",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "dev": "vite",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "vite": "7.3.6"
  }
}

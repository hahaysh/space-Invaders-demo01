# Orbit Defender 구현 계획

## 1. 범위와 채택

04-01에서 사용자 위임에 따라 PRD·TRD를 구현 가능한 작은 작업으로 나누고 아래 순서를 채택했다. 아직 게임 파일은 없다. 각 마일스톤을 별도로 구현·검증·커밋·main 게시하며 검증 실패를 다음 단계로 넘기지 않는다.

최초 범위는 REQ-01~08이다. 일시정지·난이도·목숨 및 Pages 워크플로를 선행 구현하지 않는다. 테스트 코드는 구현과 함께 작성하고 TEST_PLAN.md·TEST_RESULTS.md는 05단계에서 실제 검증과 함께 처음 정리한다.

## 2. 작업과 의존성

| 작업 | 요구·목표 | 예정 변경 파일 | 의존성 | 완료 조건 |
|---|---|---|---|---|
| M1-01 실행 기반 | REQ-08 로컬 정적 실행 | package.json, package-lock.json, .gitignore, index.html, src/style.css, vite.config.js, playwright.config.js | 승인된 PRD·TRD | Node 24에서 설치·dev·build 가능, base './', 생성물 제외 |
| M1-02 시작·이동 | REQ-01/02 | src/game.js, src/main.js, tests/game.test.js, e2e/game.spec.js | M1-01 | title/Enter·버튼, x경계/동시입력/blur 모델·브라우저 검증 |
| M1-03 발사 | REQ-03 | 위 모델·어댑터·테스트 | M1-02 | 첫 발사/0.2초/키 유지/화면 밖 탄환, Node·E2E·build 통과 |
| M2-01 편대 | REQ-04 | src/game.js, src/main.js, tests/game.test.js, e2e/game.spec.js | M1 | 24개 정확 배치·속도·생존 외곽 반전·한 번 하강 |
| M2-02 충돌·점수 | REQ-05 | 위 모델·어댑터·테스트 | M2-01 | 면적 충돌, 경계 접촉 제외, 다중 겹침에서도 중복 득점 없음 |
| M3-01 종료 | REQ-06 | 위 모델·어댑터·테스트 | M2 | 생존 적 y520 패배, 전멸240승리, 종료 후 완전 정지 |
| M3-02 재시작·전달 | REQ-07/08 | 위 파일 및 index.html/src/style.css | M3-01 | 양쪽 종료에서 R·버튼 초기화, DOM·상대 에셋·build 및 전체 회귀 |

M1은 04-02에서, M2와 M3는 04-03에서 별도 커밋으로 수행한다. 중간 M1/M2는 아직 완성 게임이 아니며 미구현 요구를 통과로 기록하지 않는다.

## 3. 실행·검증 계획

| 명령 | 예정 역할 |
|---|---|
| npm install | 04의 package.json 생성 후 의존성 설치·잠금 파일 작성 |
| npm run dev -- --host 127.0.0.1 --port 5175 --strictPort | 해당 세션의 개발 서버, 응답 확인 후 사용 |
| npm test | node --test tests/*.test.js |
| npm run test:e2e | Chromium Playwright, 전용 127.0.0.1:4175 strictPort 서버, 다른 서버 재사용 금지 |
| npm run build | dist 빌드 |
| npm run preview -- --host 127.0.0.1 --port 4176 --strictPort | dist 확인, 테스트 포트와 분리 |

위 명령은 아직 실행 전이다. 없는 의존성 오류 또는 manifest 생성 후에만 설치한다. Playwright 브라우저가 없으면 그 실패를 확인한 뒤 필요한 Chromium을 설치한다. 공개 CDN/런타임 외부 에셋을 추가하지 않는다.

모델 테스트에는 실제 PRD 수치를 사용하고 경계 바로 전·정확한 경계·경계 뒤, 정상·금지 상태를 포함한다. 브라우저 테스트는 실제 키·버튼과 제어된 시간을 사용한다. 종료 재현을 위한 개발용 모델 상태 주입은 명시적으로 구분하고 배포에서 제외한다.

## 4. 검토·게시와 위험 대응

마일스톤마다 실제 파일과 diff, 요구·테스트 연결, 공백·공개 안전을 검토하고 관련 명령을 실행한다. 실패 시 원인을 재현하여 해당 범위만 수정하고 회귀를 재실행한다. 관련 없는 파일을 커밋하지 않는다.

AGENTS의 예정 명령은 실제 스크립트와 실행 결과가 확인되면 해당 구현 단계에서 갱신한다. main 게시 직전 최신 SHA·공통 이력·공개 diff를 확인하고 일반 FF로 게시한 뒤 원격 SHA와 파일 내용을 확인한다.

원격 이력 충돌·권한 장벽은 멈추고 보고한다. 큰 디렉터리 삭제, 강제 푸시, reset/clean을 사용하지 않는다. 세션 서버만 추적하고 종료 시 해당 프로세스만 정리한다.

## 5. 현재 확인 범위

계획과 요구·설계 대응을 문서로 검토했다. 설치·게임 구현·실행 테스트·배포는 아직 미수행이다. 이후 결과는 단계 보고와 05단계 테스트 결과에 실제 증거로 기록한다.

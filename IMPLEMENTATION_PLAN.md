# Orbit Defender 구현 계획

## 1. 범위와 채택

04-01에서 사용자 위임에 따라 PRD·TRD를 구현 가능한 작은 작업으로 나누고 아래 순서를 채택했다. 당시에는 게임 파일이 없었다. 각 마일스톤을 별도로 구현·검증·커밋·main 게시했으며 현재 실제 진척과 남은 미확인은 5절에 기록한다.

최초 범위는 REQ-01~08이다. 일시정지·난이도·목숨 및 Pages 워크플로를 선행 구현하지 않는다. 테스트 코드는 구현과 함께 작성하고 TEST_PLAN.md·TEST_RESULTS.md는 05단계에서 실제 검증과 함께 처음 정리한다.

## 2. 작업과 의존성

| 작업 | 요구·목표 | 예정 변경 파일 | 의존성 | 완료 조건 |
|---|---|---|---|---|
| M1-01 실행 기반 | REQ-08 로컬 정적 실행 | package.json, package-lock.json, .gitignore, index.html, src/styles.css, vite.config.js, playwright.config.js | 승인된 PRD·TRD | Node 24에서 설치·dev·build 가능, base './', 생성물 제외 |
| M1-02 시작·이동 | REQ-01/02 | src/game.js, src/main.js, tests/game.test.js, e2e/game.spec.js | M1-01 | title/Enter·버튼, x경계/동시입력/blur 모델·브라우저 검증 |
| M1-03 발사 | REQ-03 | 위 모델·어댑터·테스트 | M1-02 | 첫 발사/0.2초/키 유지/화면 밖 탄환, Node·E2E·build 통과 |
| M2-01 편대 | REQ-04 | src/game.js, src/main.js, tests/game.test.js, e2e/game.spec.js | M1 | 24개 정확 배치·속도·생존 외곽 반전·한 번 하강 |
| M2-02 충돌·점수 | REQ-05 | 위 모델·어댑터·테스트 | M2-01 | 면적 충돌, 경계 접촉 제외, 다중 겹침에서도 중복 득점 없음 |
| M3-01 종료 | REQ-06 | 위 모델·어댑터·테스트 | M2 | 생존 적 y520 패배, 전멸240승리, 종료 후 완전 정지 |
| M3-02 재시작·전달 | REQ-07/08 | 위 파일 및 index.html/src/styles.css | M3-01 | 양쪽 종료에서 R·버튼 초기화, DOM·상대 에셋·build 및 전체 회귀 |

M1은 04-02에서, M2와 M3는 04-03에서 별도 커밋으로 수행한다. 중간 M1/M2는 아직 완성 게임이 아니며 미구현 요구를 통과로 기록하지 않는다.

## 3. 실행·검증 계획

| 명령 | 예정 역할 |
|---|---|
| npm install | 04의 package.json 생성 후 의존성 설치·잠금 파일 작성 |
| npm run dev -- --host 127.0.0.1 --port 5173 --strictPort | 원문 개발·E2E 서버, 응답 확인 후 사용 |
| npm test | node --test tests/*.test.js |
| npm run test:e2e | Chromium 프로젝트, 127.0.0.1:5173 strictPort 서버, 다른 서버 재사용 금지 |
| npm run build | dist 빌드 |
| npm run preview -- --host 127.0.0.1 --port 4173 --strictPort | dist 확인, 개발·E2E 포트와 분리 |
| npm ci | 잠금 파일을 사용한 이후 재현 설치; 현재 설치가 있어 이번 보정에는 미실행 |

04-01 채택 당시에는 위 명령을 아직 실행하지 않았다. 실제 실행 결과는 5절과 AGENTS의 최신 명령 기준을 사용한다. 없는 의존성 오류 또는 manifest 생성 후에만 설치한다. Playwright 브라우저가 없으면 그 실패를 확인한 뒤 필요한 Chromium을 설치한다. 공개 CDN/런타임 외부 에셋을 추가하지 않는다.

모델 테스트에는 실제 PRD 수치를 사용하고 경계 바로 전·정확한 경계·경계 뒤, 정상·금지 상태를 포함한다. 상태 주입은 순수 모델 단위 테스트에만 사용하며 개발·배포 브라우저에 전역 상태 변경·즉시 승리·테스트 전용 상태 API를 제공하지 않는다. 브라우저 테스트는 실제 키·버튼과 제어된 시간으로 진행하고 DOM·Canvas를 읽기 전용으로 관찰한다. 이 문구는 초기 개발 훅 허용 해석을 바로잡은 것이며 제품 규칙 변경이 아니다.

## 4. 검토·게시와 위험 대응

마일스톤마다 실제 파일과 diff, 요구·테스트 연결, 공백·공개 안전을 검토하고 관련 명령을 실행한다. 실패 시 원인을 재현하여 해당 범위만 수정하고 회귀를 재실행한다. 관련 없는 파일을 커밋하지 않는다.

AGENTS의 예정 명령은 실제 스크립트와 실행 결과가 확인되면 해당 구현 단계에서 갱신한다. main 게시 직전 최신 SHA·공통 이력·공개 diff를 확인하고 일반 FF로 게시한 뒤 원격 SHA와 파일 내용을 확인한다.

원격 이력 충돌·권한 장벽은 멈추고 보고한다. 큰 디렉터리 삭제, 강제 푸시, reset/clean을 사용하지 않는다. 세션 서버만 추적하고 종료 시 해당 프로세스만 정리한다.

## 5. 현재 확인 범위

아래는 2026-09-14의 실제 작업 기록을 늦게 반영한 것이다. 일반적인 내용 채택·게시 진행은 사용자 위임에 따라 에이전트가 수행했으며 사람이 직접 조작했다고 기록하지 않는다.

조정자의 통합 정렬 검증 승인 전 시나리오 집계는 9/25였다. 2bfaee1의 정렬과 759e064의 통합 결과를 수락한 뒤 04-02/04-03을 포함한 11/25로 갱신되었다. 아래 표는 당시의 기능 구현·검증·게시 이력을 보존한다.

| 단계 | 실제 구현·자동 검증 | 게시 근거 | 남은 확인 |
|---|---|---|---|
| 04-02 M1 | 시작·이동·발사만 구현, 당시 npm test 8/8·E2E 6/6·build pass | 35f39b9, M2 이전 별도 게시 | 사람 직접 조작·OS포커스 unverified |
| 04-03 M2 | 적·충돌·점수만 추가, 당시 15/15·9/9·build pass | 2403616, M3 이전 별도 게시 | 사람의 적 편대·점수 관찰 unverified |
| 04-03 M3 | 승패·전체 재시작, 당시 20/20·18/18·build pass | c28f6bd, 별도 게시 | 사람 완주·직접 재시작 unverified |
| 문서·원문 해석 보정 | TRD 승인 위치 보완, 브라우저 상태 훅 제거 및 실제 키로 승패 재현 | 391987a·8690448, 기존 이력 유지 | 늦게 도착한 보정이며 이전부터 원문과 일치했다고 주장하지 않음 |

원문의 고정 커밋 c545b103a8816a061bb754d9ca92f59d25cfa7a9에서 04-02/04-03 전문을 gh api로 확인한 뒤 스타일 경로·명령·포트·허용 파일 경로를 추가 보정했다. 5173/4173 LISTEN 점유가 없으므로 대체 포트 근거는 없다. 현재 자동 검증은 npm test 22/22·Chromium E2E 14/14·build pass이며 개발 5173과 preview 4173을 구분한다. 상태 주입 없이 실제 키·제어 시간으로 240점 승리와 자연 패배, 양쪽 R/버튼 재시작을 확인했다.

원문의 참가자 직접 확인 항목과 Firefox/WebKit은 unverified다. 이후 실제 QA 진행은 다음 절에 기록하며 과거의 정렬 대기 상태로 다음 단계를 차단하지 않는다.

## 6. 05 실제 진행

05-01에서 요구사항별 TEST_PLAN.md를 검토·작성한 다음 실제 Node22/E2E14/build와 별도 preview 브라우저를 실행하고 TEST_RESULTS.md R1에 기록했다. 5ec4dea에 두 문서만 게시했다.

05-02에서 BUG-01 favicon 누락을 실패 재현→인라인 SVG 최소 수정→같은 검사 및 전체 회귀 순서로 해결하여 d97a6d1에 게시했다. GAP-01은 제품 결함이 아닌 반복 재시작 자동화 누락으로 분류하고 e2e/game.spec.js만 보강했다. 10회 재시작 전후 입력·시간·구독 수를 실제 Chromium에서 반복 확인했으며 R3에 Node22/E2E15/build의 최신 결과와 남은 미확인을 기록한다. 제품 규칙 변경이나 P/난이도/목숨 선행 구현은 없다.

필수 로컬 검사에는 열린 실패·차단이 없다. 다음은 06의 실제 Pages 설정·CI·공개 URL 검증이다. 사람 직접 플레이·OS 포커스는 자동 브라우저와 구분하며 일반적인 다음 단계 판단은 사용자 위임에 따른다.

## 7. CHG-01 작업 계획 (07-02 채택)

06 최초 배포와 실제 공개 브라우저 확인은 TEST_RESULTS R4에 완료되어 있고 07-01 요청은 CHANGE_REQUEST.md에 있다. 아래는 07-02에서 채택한 작업과 07-05까지의 실제 상태다.

| 작업 | 기준·변경 파일 | 의존성 | 완료 조건 | 실제 상태 |
|---|---|---|---|---|
| P1 모델 전환·정지 | AC1/3, src/game.js와 tests/game.test.js | 07-02 문서 채택 | paused 전환·금지 상태·전체 모델/쿨다운 동결의 결정적 검사 | 07-03 A 완료, Node25/25 |
| P2 입력·시간·표시 | AC2/4/5/6/7, src/main.js 및 필요한 index.html/src/styles.css, e2e/game.spec.js | P1 | repeat 무시·입력 해제·긴 정지 후 재개·안내·단일 RAF, 새 버튼 없음 | 07-03 B 완료, E2E17/17·build pass. CSS 수정 불필요 |
| P3 회귀·실제 결과 | AC1~8, 두 테스트 파일·TEST_RESULTS.md, 해당 계획 상태 | P1/P2 | 10회 전환과 기존 REQ01~08, Node/E2E/build, 실패 보존·최소 수정 | 07-04 완료, R6의 기준별 표·Node25/E2E17/build·별도 preview 확인 |
| P4 공개 확인 | AC8, TEST_RESULTS.md | P3와 성공한 main 배포 | 실제 배포 SHA/URL에서 P와 기존 게임 동작 검증 | 07-05 완료, 성공 배포 e941cf3 및 실제 공개 Chromium·아티팩트 바이트 대조, R7 |

07-03에서 작은 모델/어댑터 구현을 수행하고 07-04에서 전체 회귀·결과, 07-05에서 공개 검증을 기록한다. 문서 main 게시로 실행될 수 있는 현재 최초 게임의 배포를 P 구현 완료로 세지 않는다.

07-03은 A의 두 파일만 수정하고 npm test 25/25와 diff를 먼저 확인한 뒤 B에 진입했다. B는 기존 입력 해제·시간 기준 초기화·단일 RAF·오버레이를 재사용했으며 새 루프·버튼·브라우저 상태 API를 만들지 않았다. 관련 Chromium 6개를 먼저 확인하고 Node25/E2E17/build를 다시 통과했다. 기존 playing에서 P 무시 검사만 새 요구에 맞게 대체했고 Enter/R 금지·종료 후 P 금지와 기존 수치 검사는 유지했다.

## 8. CHG-02 작업 계획 (08-01 채택)

기준은 CHG-01 완료와 기록 재배포가 확인된 24600403이다. 08-01 설계는 bdec601로 실제 원격 demo에 게시했으며 아래는 08-02 공개 확인까지의 상태다. app 로컬 브랜치는 그대로 유지한다.

| 작업 | 기준·허용 파일 | 의존성·검증 | 현재 상태 |
|---|---|---|---|
| D1 공통 설정·모델 | AC1/2/3/4/6/7/9/10, src/game.js·tests/game.test.js | 설정/기본값·명시적 오류·선택/게임 구분·세 속도·양 경계·P, npm test 후 diff 검토 | 완료, A Node29/29와 실제 diff 검토 |
| D2 선택 UI | AC4/5/8/9, index.html·src/styles.css·src/main.js·e2e/game.spec.js | D1 후 네이티브 select·표시·잠금·조작된 이벤트·새로고침·오류, Node/E2E/build | 완료, 전체 Node29/E2E24/build, R8 |
| D3 회귀·게시 확인 | AC1~10, 필요한 계획/결과/요청 문서 | D1/D2 후 기존 REQ01~08·CHG-01, 별도 preview, demo 게시·안전한 main FF 통합·실제 공개 확인 | 완료, R8 로컬·R9 demo 검사 및 main 일반 FF·실제 배포/공개 검증 |

새 의존성·워크플로·목숨 기능은 추가하지 않는다. main 통합 전에는 난이도가 공개 배포되었다고 기록하지 않으며 환경의 main 제한을 완화하지 않는다.

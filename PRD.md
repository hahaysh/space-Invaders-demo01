# Orbit Defender 제품 요구사항

## 1. 목표와 결정 상태

키보드 브라우저 게임 초보자가 좌우 이동·발사로 적 편대를 줄이고 승패 뒤 다시 도전하는 한 화면 게임을 만든다. 제목은 Orbit Defender, 테마는 어두운 우주와 밝은 자체 도형이다. ideation.md에서 선택한 공통 기준을 유지한다.

02-02에서 사용자의 자율 진행 위임에 따라 에이전트가 아래 REQ-01~REQ-08, 수치와 D-01~D-08 권장안을 채택했다. 이는 사용자의 항목별 직접 승인이나 게임 구현·실행 검증 완료를 뜻하지 않는다.

## 2. 사용자 흐름과 상태

최초 상태는 title이다. 최초 배포의 title/playing/won/lost에 CHG-01의 paused와 09-01 CHG-03의 retry를 추가한다. 아래는 09-01에서 채택한 규칙이며 당시에는 목숨 구현·검증 전이었다. 최초 규칙과 변경 이유는 10절, 이후 실제 구현·검증 상태는 TEST_RESULTS.md의 09-02 회차에 보존한다.

| 현재 상태 | 입력·조건 | 다음 상태와 관찰 결과 |
|---|---|---|
| title | Enter 또는 시작 버튼 | playing, 점수 0과 초기 한 판 상태. 선택 난이도를 이번 게임에 적용 |
| title/won/lost | 난이도 선택 | 상태 유지, 다음 게임의 선택값만 변경 |
| playing/paused/retry | 난이도 선택 시도 | 선택 UI 비활성, 이번 게임 설정 변경 없음 |
| title | 이동·Space·R | title 유지. 이동·발사·재시작 없음 |
| playing | 좌우 방향키·A/D 또는 Space | playing에서 이동·발사 수행 |
| playing | Enter·R | 현재 판 유지. 초기화 없음 |
| playing | 반복이 아닌 P | paused, 모든 게임 진행 중지·입력 해제 |
| paused | 반복이 아닌 P | playing, 새 입력부터 처리하며 정지 시간만큼 이동하지 않음 |
| paused | 이동·Space·Enter·R, 반복 P | paused 유지. 시뮬레이션·점수·발사 대기시간 불변 |
| playing | 충돌 반영 후 생존 적 중 하단이 520 이상 | 목숨1 감소. 남은2/1은 retry, 0은 lost. 실패 점수 보존·시뮬레이션 중지 |
| playing | 충돌 반영 후 생존 적 0개 | won, 목숨 미차감·최종 점수 보존 |
| retry | 반복이 아닌 새 Enter 또는 재도전 버튼 | playing, 남은 목숨·현재 난이도 보존, 점수0을 포함한 시도 초기화 |
| retry | 시간·반복 Enter·R/P·이동·발사 | retry 유지, 모든 시뮬레이션 값과 점수 고정 |
| won/lost | R 또는 재시작 버튼 | 선택 난이도·목숨3·점수0인 새 playing 시작 |
| won/lost | 이동·Space·Enter | 상태·점수 유지. 이동·발사·새 판 없음 |

상태에 맞지 않는 시작·재시작 조작으로 진행 중인 판을 초기화하지 않는다.

## 3. 공통 기준과 수치

좌표는 Canvas 논리 픽셀(px), 원점은 좌상단이다. x는 오른쪽, y는 아래쪽으로 증가한다. 객체 위치는 좌상단, 크기는 너비×높이, 속도는 px/s, 시간은 초(s)다.

| 항목 | 채택 값 | 근거·연결 |
|---|---|---|
| Canvas | 800×600 px | ideation 공통 기준. REQ-02/08 |
| 기본 조작 | 좌우 방향키 또는 A/D, Space 발사, Enter/버튼 시작, 종료 후 R/버튼 재시작 | ideation 공통 기준. REQ-01/02/03/07 |
| 플레이어 | 40×20 px, 초기 (380,550), y=550 고정, 속도 320 px/s, x 범위 [0,760] | PRD 권장안 채택. REQ-02/07 |
| 탄환 | 4×12 px, 위쪽 600 px/s, 플레이어 윗변 가로 중앙에서 발사 | PRD 권장안 채택. REQ-03 |
| 탄환 생성 위치 | 좌상단 (플레이어 x+18,538), 하단이 플레이어 윗변 y=550과 맞닿음 | 중앙·크기 기준의 구체화. REQ-03 |
| 발사 간격 | Space 유지 시 0.2초 간격, 시작·재시작 후 첫 발사는 즉시 가능 | 권장안 채택. REQ-03/07 |
| 적 수 | 3행×8열=24개, 단일 웨이브 | ideation 공통 기준. REQ-04/06 |
| 적 크기·기본 이동 | 40×24 px, 기본 보통은 수평 64 px/s, 최초 오른쪽, 반전 시 하강 24 px | REQ-04/07. 08-01 난이도 확장은 9절 |
| 적 초기 위치 | (112+열×72,72+행×48), 행 0~2·열 0~7 | PRD 권장안 채택. REQ-04/07 |
| 점수 | 시도 초기0, 적당10, 시도별 최대240. retry 대기 중 실패 점수 보존 | REQ-01/05/07, CHG-03은 재도전 시작 때0 |
| 방어선 도달 | 생존 적의 하단 y+24 >= 520 | 최초 즉시 패배에서 CHG-03의 목숨 감소로 확장. REQ-06 |
| 목숨 | 새 게임3, 한 번 도달에1 감소, 0일 때만 lost | CHG-03, REQ-06/07 |
| 충돌 | 사각형 면적이 겹칠 때만 충돌. 경계만 접촉하면 비충돌 | 권장안 채택. REQ-05 |

## 4. 필수 요구사항과 수용 기준

모든 요구사항의 우선순위는 필수(Must)다. 수치는 3절을 기준으로 한다.

| ID·목표 | 우선순위 | 정상 수용 기준 | 경계 수용 기준 | 금지 동작 |
|---|---|---|---|---|
| REQ-01 시작 | 필수 | 처음 title에서 제목·조작 안내·시작 버튼을 제공한다. Enter/시작 버튼으로 점수 0인 초기 playing이 된다. | title에서 R을 무시한다. playing 중 시작 입력으로 판을 초기화하지 않는다. | 입력 없는 자동 시작, title에서 게임 진행·이동·발사 |
| REQ-02 이동 | 필수 | playing에서 좌우/A·D로 320 px/s 수평 이동하며 y=550을 유지한다. | x는 0~760을 벗어나지 않는다. 왼쪽 계열과 오른쪽 계열 입력이 동시에 활성화되면 정지한다. | title/paused/retry/won/lost에서 이동, 수직 이동, 화면 밖 이동 |
| REQ-03 발사 | 필수 | playing에서 Space로 탄환을 생성하여 위로 600 px/s 이동시킨다. 유지하면 0.2초 간격으로 반복한다. | 새 판 첫 발사는 대기 없이 가능하다. 발사 후 0.2초 미만에 추가 발사하지 않는다. 탄환 하단이 0 이하이면 제거한다. | playing 밖 발사, 키 반복에 따른 발사 간격 단축, 명중 소모·화면 밖 탄환 유지 |
| REQ-04 적 편대 | 필수 | 지정 배치의 24개 적이 오른쪽으로 출발한다. 기본 보통은 이전 64 px/s, CHG-02는 선택에 따라 32/64/96 px/s로 수평 이동한다. | 생존 편대 왼끝 0 또는 오른끝 800 도달 시 화면 안에서 반전하고 생존 적 전체가 24 px 하강한다. 한 번의 경계 도달에 하강은 한 번이다. | 화면 밖 편대 유지, 경계 도달 없이 하강, 자동 가속·추가 웨이브·추가 적 생성 |
| REQ-05 충돌·점수 | 필수 | 탄환과 적의 사각형 면적이 겹치면 적 1개 제거, 탄환 즉시 소모, 10점 증가가 발생한다. | 경계 접촉만으로 명중하지 않는다. 탄환 하나는 최대 적 하나만 제거한다. 같은 적에 여러 탄환이 겹쳐도 그 적 점수는 한 번이다. 전멸 시 240점이다. | 한 탄환의 다중 제거, 같은 적 중복 득점, 빗나감 득점, 240점 초과 |
| REQ-06 종료·목숨 | 필수 | 충돌·득점 후 생존 적 하단>=520이면 목숨1 감소, 남으면 retry·0이면 lost. 생존 적이 없으면 미차감 won이다. | 하단520 미만 미차감, 정확히/이상은 차감. 여러 위험 적·서브스텝에도 한 번만 차감. 제거된 적은 제외하고 다른 생존 위험 적 도달을 우선한다. | 음수 목숨·중복 차감·제거된 적 때문에 차감, retry/종료 중 시뮬레이션·점수 갱신 |
| REQ-07 새 게임·재도전 | 필수 | won/lost의 R/재시작은 선택 난이도·목숨3. retry의 새 Enter/재도전은 남은 목숨·현재 난이도 보존. 모두 위치·24적·탄환 없음·점수0·오른쪽 방향·입력·시간·쿨다운 초기화 | 실패 점수는 retry 대기 중 보존하고 시작 때0. 첫 발사는 즉시 가능하며 새 판/재도전 뒤 이전 입력·발사 대기 없음 | title의 R·playing/paused의 Enter/R·retry의 R/P/반복 Enter로 초기화, retry 목숨 복구·난이도 변경·점수 누적 |
| REQ-08 전달 | 필수 | 정적 데스크톱 게임에 시작·재시작·재도전 버튼, 상태·점수·남은 목숨을 DOM 텍스트로 제공한다. | 저장소 하위 URL에서도 상대 에셋으로 동작. won/lost는 최종 점수와 남은 목숨, retry는 실패 점수와 재도전 안내 표시. 배포물은 dist뿐 | 정보를 Canvas 픽셀만으로 전달, 외부 CDN·게임 엔진·이미지 서버 의존, 원본 개발 문서 배포 |

## 5. 제외 범위와 후속 변경

최초 버전에서는 목숨, 적 탄환, 사운드, 모바일 조작, 온라인 순위, 로그인·서버 기능, 여러 웨이브, 테마 선택, 외부 에셋과 paused를 제외했다. CHG-01/02는 각각07/08에서 구현·공개 검증했고 09-01에서는 CHG-03 목숨·재도전 설계만 채택한다. 다른 제외 범위는 유지한다.

| 단계·변경 | 후속 범위 | 최초 버전과의 경계 |
|---|---|---|
| 07 / CHG-01 | P 키 일시정지·재개 | 최초 미지원, 07-02 설계 → 07-03 구현 → 07-05 공개 검증 완료 |
| 선택 확장 08 / CHG-02 | 난이도 선택 | 08-01 요구·설계 채택, 구현은 08-02. 자동 레벨 상승 없음 |
| 선택 확장 09 / CHG-03 | 목숨3·수동 재도전 | 09-01 문서 설계 채택, 09-02 구현 예정 |

## 6. 채택한 결정과 이유

아래 결정은 사용자의 자율 진행 위임에 따라 에이전트가 채택했다.

| 결정 | 채택안 | 이유 |
|---|---|---|
| D-01 요구·수치 | REQ-01~08 필수, 3절 수치 채택 | 공통 실습·후속 회귀 기준 유지. 적 속도·하강 폭 변경 시 관련 후속 검사도 조정 |
| D-02 발사 | Space 유지 0.2초 간격, 새 판 첫 발사 즉시 | 키 반복 속도와 독립적인 조작 |
| D-03 동시 이동 | 양 방향 계열 동시 입력 시 정지 | 상충 입력 결과의 일관성 |
| D-04 충돌 | 면적 겹침만 명중, 경계 접촉 제외 | 접촉 경계의 모호함 해소 |
| D-05 종료 | 충돌 후 생존 적으로 lost 검사 후 won | 제거된 적의 패배 유발 방지 |
| D-06 상태 입력 | playing Enter/R, title R 무시. 종료 후 R/버튼 재시작 | 예상 밖 초기화 방지 |
| D-07 탄환 경계 | 탄환 하단을 플레이어 윗변 중앙에 맞추고 하단 <= 0이면 제거 | 생성·제거의 관찰 가능한 조건 |
| D-08 편대 경계 | 생존 적 외곽 0/800에서 한 번 반전·24 px 하강, 화면 안 유지 | 편대 축소 후에도 명확한 경계 |

현재 최초 버전 구현을 막는 미결정 제품 규칙은 없다. 변경 시 관련 요구사항과 후속 검사의 영향을 함께 검토한다.

## 7. 검증 연결과 문서 완료 기준

TRD·구현 계획·테스트는 REQ-01~REQ-08을 참조하고 정상·경계·금지 조건을 검증 항목에 연결한다. 실제 결과는 pass/fail/unverified, 미실행·환경 차단은 별도 실행상태·사유로 기록한다.

02-02에서는 요구·수치·결정을 위임에 따라 채택하고 문서 정합성을 확인했다. 게임 실행·테스트·사용성·배포 검증은 아직 수행하지 않았다. 함수·코드 구조·충돌 검사 알고리즘·시간 갱신 방식·실행 명령은 TRD와 구현 단계에서 다룬다.

## 8. CHG-01 일시정지 수용 기준

CHANGE_REQUEST.md의 CHG-01-AC1~8을 제품 요구로 채택한다. P는 playing↔paused에서만 유효하며 title/retry/won/lost와 키 자동 반복에서는 무효다. paused 동안 플레이어·적·탄환·점수·목숨·발사 대기시간과 게임 시간이 모두 보존되고, 일시정지 및 P 재개 안내를 DOM에도 제공한다.

blur·상태 전환은 유지 입력을 해제하지만 blur만으로 자동 정지하지 않는다. 재개는 정지 시간을 한꺼번에 반영하지 않고 새 입력부터 처리한다. 새 재개 버튼은 없다. REQ-01~08의 기존 수치·충돌·종료 기준을 바꾸거나 요구를 삭제하지 않는다. 시간·루프 구현 방법은 TRD가 담당한다.

## 9. CHG-02 난이도 선택 수용 기준

08-01에서 사용자 위임으로 CHANGE_REQUEST.md의 CHG-02-AC1~10을 채택했다. 이는 아직 난이도 구현·검증 완료가 아니다.

| 키·표시 | 적 수평 속도 | 기본값 |
|---|---|---|
| easy · 쉬움 | 32 px/s | 아니오 |
| normal · 보통 | 64 px/s | 최초/새로고침/미지정 생성 |
| hard · 어려움 | 96 px/s | 아니오 |

title/won/lost에서만 다음 게임의 난이도를 선택한다. 시작·재시작 시 해당 선택을 이번 게임의 고정 설정으로 적용한다. playing/paused와 CHG-03의 retry에서는 UI와 모델 모두 변경을 막으며 조작된 DOM 값도 반영하지 않는다. 지원하지 않는 값은 명시적으로 오류를 표시하고 normal로 조용히 대체하지 않는다.

label이 연결된 기본 select를 제공하고 그 안의 방향키·Enter·P는 네이티브 조작으로 남긴다. title에서는 선택 난이도를, 게임 시작 뒤에는 이번 게임 난이도를 텍스트로 표시하여 다음 선택과 구분한다. 브라우저 저장은 하지 않는다.

CHG-02 자체는 REQ-04의 수평 속도만 확장했다. 24적·24px 하강·10점·최대240점·0.2초 발사·플레이어320/탄환600 속도·y520·단일 웨이브와 CHG-01은 모든 난이도에서 유지한다. 목숨은 다음 CHG-03에서만 추가하며 적 탄환·새 웨이브·자동 레벨은 제외한다.

## 10. CHG-03 변경 이력과 목숨 수용 기준

09-01에서 사용자 위임으로 CHANGE_REQUEST.md의 CHG-03-AC1~13을 채택했다. 최초 REQ-06/D-05는 방어선 도달 즉시 lost였으나, 현재 규칙은 충돌 후 한 번 차감하여 남은2/1은 retry·0만 lost다. 최초 REQ-07/D-06의 종료 후 완전 재시작을 보존하면서, 남은 목숨·현재 난이도를 유지하는 별도 재도전을 추가한다. 최초 수용·실행 이력은 이전 커밋과 TEST_RESULTS에 보존한다.

retry의 실패 점수는 대기 중 표시·보존하고 새 Enter/재도전 버튼을 선택할 때0으로 만든다. 편대 복구와 점수 누적으로240을 넘기는 정책은 채택하지 않는다. retry의 이동·탄환·쿨다운·목숨은 고정이며 새로 누른 Enter와 버튼만 유효하다. R로 목숨3을 복구하거나 난이도를 바꾸는 우회는 없다.

재도전은 같은 단일 웨이브의 새 시도이며 새 게임/종료 후 재시작과 다르다. won은 당시 남은 목숨과 최종 점수, lost는0목숨과 최종 점수를 표시한다. 목숨은 '남은 목숨: 3' 같은 텍스트로 제공한다. 게임 규칙·수치의 나머지 변경은 없으며 구현·실제 검증은09-02에서 수행한다.
